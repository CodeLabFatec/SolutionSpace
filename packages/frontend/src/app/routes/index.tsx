import { BrowserRouter, Navigate, Route, Routes } from "react-router-dom"

export const Caminhos = () => {
    return(
        <BrowserRouter>
            <Routes>
            </Routes>
        </BrowserRouter>
    )
}